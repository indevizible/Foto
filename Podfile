#pod
platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!

target "FotoDemo" do
    pod 'Fusuma', :git => 'https://github.com/indevizible/Fusuma.git',:branch => 'camera-only', :tag => '1.0.1.1'
end
